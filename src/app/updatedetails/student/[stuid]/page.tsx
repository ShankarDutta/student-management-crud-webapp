import { Metadata } from "next";

export const metadata: Metadata = {
	title: "Update Student Details | Student Management CRUD App",
	description:
		"Update Student page in the Next.js Student Management CRUD system. Modify and save existing student information through a smooth, responsive form designed for efficient data management.",
};
const page = () => {
	return <div>Update Student Details</div>;
};

export default page;
