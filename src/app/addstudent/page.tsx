import { Metadata } from "next";

export const metadata: Metadata = {
	title: "Add Student | Student Management CRUD App",
	description:
		"Add Student page for the Student Management CRUD system built with Next.js. Use the simple and responsive form to enter and save new student details efficiently, ensuring smooth and organized student data management.",
};

const page = () => {
	return <div>Add Students</div>;
};

export default page;
