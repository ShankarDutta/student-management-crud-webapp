import { Metadata } from "next";

export const metadata: Metadata = {
	title: "All Techers | Student Management CRUD App",
	description:
		"Dedicated Teachers List page in the Next.js Teacher Management CRUD system. Displays all teacher profiles in an organized, responsive layout with quick actions to edit or delete teacher information for smooth data management.",
};

const page = () => {
	return <div>All Teachers</div>;
};

export default page;
