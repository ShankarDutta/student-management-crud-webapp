import { Metadata } from "next";

export const metadata: Metadata = {
	title: "Add Teacher | Student Management CRUD App",
	description:
		"Add Teacher page for the Teacher Management module in the Next.js CRUD Web App. Easily create and save new teacher records using a clean, responsive form designed for fast and efficient data entry.",
};
const page = () => {
	return <div>Add teacher</div>;
};

export default page;
